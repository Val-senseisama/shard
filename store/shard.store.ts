// This store was removed — Apollo Client is the single source of truth for shard data.
// Use useQuery(GET_SHARD, { variables: { id } }) or useQuery(MY_SHARDS) directly in components.
// For navigation between screens, pass the shard ID as a route param.
export {};
